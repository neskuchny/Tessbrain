-- SIMA Фаза 2: «что берём из источника» на кубике.
-- Видение: «в каждом кубике я должен иметь возможность указать что оттуда
-- взять» — короткая заметка на теле кубика-источника (встреча/док/текст):
-- что именно из него идёт в синтез. Отдельное поле, не путать с input_data
-- (входные данные блока-модуля) — здесь это извлекаемая суть источника.
ALTER TABLE sima_blocks
    ADD COLUMN IF NOT EXISTS source_take TEXT;
